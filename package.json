{
  "name": "presentr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./node_modules/.bin/browser-sync start --server --files '**/*.css, **/*.html, **/*.js, !node_modules/**/*' --directory --port 1313"
  },
  "author": "Philipp Nowinski <philipp@nowinski.de>",
  "license": "MIT",
  "dependencies": {
    "browser-sync": "^2.23.5"
  }
}
